# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: salesorderapp.ui
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: S4HANA
          url: http://s4hana2022.bas:8128
          client: '800'
        app:
          name: ZFIORI_PRODUCTS
          description: Manage Product Application
          package: ZFIORI
          transport: TTAK903397
        exclude:
          - /test/
          - /localService/
